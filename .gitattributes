# Git Attributes Configuration
# This file defines attributes for pathnames to control how Git handles files

################################################################################
# Auto-detect text files and normalize line endings to LF
################################################################################
* text=auto eol=lf

################################################################################
# Explicitly declare text files
################################################################################
*.php text eol=lf
*.js text eol=lf
*.css text eol=lf
*.scss text eol=lf
*.json text eol=lf
*.xml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
*.md text eol=lf
*.txt text eol=lf
*.sql text eol=lf
*.sh text eol=lf
*.bash text eol=lf

# Configuration files
*.conf text eol=lf
*.config text eol=lf
*.ini text eol=lf
.env* text eol=lf
.gitignore text eol=lf
.gitattributes text eol=lf
.editorconfig text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf

# Documentation
*.rst text eol=lf
LICENSE text eol=lf
README text eol=lf
CHANGELOG text eol=lf

################################################################################
# Declare binary files (will not be modified)
################################################################################
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg binary
*.pdf binary
*.zip binary
*.tar binary
*.gz binary
*.tgz binary
*.rar binary
*.7z binary
*.phar binary
*.exe binary
*.dll binary
*.so binary
*.dylib binary
*.woff binary
*.woff2 binary
*.ttf binary
*.eot binary
*.otf binary

################################################################################
# Git LFS Configuration (Informational)
################################################################################
# 
# Git LFS (Large File Storage) is recommended for files larger than 100MB
# To enable Git LFS tracking, uncomment the patterns below and install Git LFS
# 
# Installation:
#   Ubuntu/Debian: sudo apt-get install git-lfs
#   macOS: brew install git-lfs
#   Then run: git lfs install
#
# Uncomment patterns below to track with Git LFS:

# Large archives in vendor
# vendor/**/*.zip filter=lfs diff=lfs merge=lfs -text
# vendor/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text
# vendor/**/*.phar filter=lfs diff=lfs merge=lfs -text

# Large documentation files
# vendor/**/docs/**/*.pdf filter=lfs diff=lfs merge=lfs -text
# *.pdf filter=lfs diff=lfs merge=lfs -text

# Large datasets (if any)
# *.csv filter=lfs diff=lfs merge=lfs -text
# *.sql filter=lfs diff=lfs merge=lfs -text

################################################################################
# Diff settings for specific file types
################################################################################

# PHP - use PHP-specific diff driver if available
*.php diff=php

# JSON - treat as text with pretty formatting
*.json diff

# SQL - treat as text
*.sql diff

################################################################################
# Merge strategies
################################################################################

# Composer lock file - conflicts should be resolved by regenerating
# Use: rm composer.lock && composer install
composer.lock merge=binary

# Package manager lock files - regenerate on conflict
package-lock.json merge=binary
yarn.lock merge=binary

################################################################################
# Export settings (files to exclude from archive exports)
################################################################################

# Development files not needed in exports
.gitattributes export-ignore
.gitignore export-ignore
.github export-ignore
tests/ export-ignore
.editorconfig export-ignore
phpunit.xml.dist export-ignore
docker-compose.yml export-ignore
Dockerfile export-ignore

################################################################################
# Vendor directory settings
################################################################################

# Ensure vendor files are treated as binary when appropriate
# This helps prevent line ending issues in generated code
vendor/**/*.php text eol=lf
vendor/**/*.json text eol=lf
vendor/**/*.md text eol=lf

# Binary files in vendor
vendor/**/*.phar binary
vendor/**/*.zip binary
vendor/**/*.tar.gz binary
vendor/**/*.exe binary

################################################################################
# Linguist settings (for GitHub language statistics)
################################################################################

# Exclude vendor from language statistics
vendor/* linguist-vendored
vendor/**/* linguist-vendored

# Exclude test fixtures
tests/fixtures/* linguist-vendored

# Documentation language
*.md linguist-documentation
docs/* linguist-documentation
